import React from 'react';

import { useHistory } from 'react-router-dom';
 

const AdressRegister= () => {
  const history = useHistory()

  return <div>

Adress Register
  </div>
}

export default AdressRegister;